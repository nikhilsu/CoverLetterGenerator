<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Cover Letter Gen</title>
    <link rel="apple-touch-icon" sizes="180x180" href="images/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="images/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="images/favicon-16x16.png">
    <link rel="manifest" href="images/site.webmanifest">

    <link rel="stylesheet" type="text/css" href="css/index.css"/>
    <script src="https://code.jquery.com/jquery-1.9.1.min.js"></script>
    <script src="javascript/index.js" type="text/javascript"></script>
</head>
<body>

<% if (locals.headlineError) { %>
    <div class="headline-error"> Something went wrong! try again later.</div>
<% } %>

<div class="container">
    <p class="heading">Cover Letter Generator</p>

    <form action="/" method="post">
        <div class="input">
            <input type="text" placeholder="Position" name="position" value="<%= locals.position %>"
            <% if (locals.error && locals.error.position) { %>
                   class="error-input" />
            <p class="error-message">
                <%= error.position %>
            </p>
            <% } else { %>
                />
            <% } %>
        </div>
        <div class="input">
            <input type="text" placeholder="Company" name="company" value="<%= locals.company %>"
            <% if (locals.error && locals.error.company) { %>
                   class="error-input"/>
            <p class="error-message">
                <%= error.company %>
            </p>
            <% } else { %>
                />
            <% } %>
        </div>
        <div class="input">
            <button type="submit">Download</button>
        </div>
    </form>
</div>
</body>
</html>